import { CustomerAccessPanel } from '@/components/customer-access-panel';

export default function AccessPage() {
  return (
    <CustomerAccessPanel description="Enter a valid customer token to unlock dashboard counts, filtered article views, and customer-only CSV export from the Explorer." />
  );
}
