import { CustomerAccessPanel } from '@/components/customer-access-panel';

export default function AccessPage() {
  return (
    <CustomerAccessPanel description="Enter a valid customer token to unlock map country/source detail and private portal features." />
  );
}
