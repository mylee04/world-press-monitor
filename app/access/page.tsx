import { CustomerAccessPanel } from '@/components/customer-access-panel';

export default function AccessPage() {
  return (
    <CustomerAccessPanel description="Enter a valid customer token to unlock dashboard counts, benchmark views, and other restricted customer portal data." />
  );
}
