import { CustomerAccessPanel } from '@/components/customer-access-panel';

export default function AccessPage() {
  return (
    <CustomerAccessPanel description="Enter a valid customer token to unlock dashboard counts, benchmark views, map intelligence, and other customer-only portal data." />
  );
}
