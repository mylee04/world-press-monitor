// Placeholder drizzle schema file.
//
// If you already have an existing PostgreSQL schema (db/schema.sql),
// generate Drizzle schema via:
//   bun run db:introspect -- --url "$DATABASE_URL" --out ./db/drizzle
//
// Or define tables manually using drizzle-orm.
export {};

